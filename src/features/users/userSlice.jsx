import { createSlice } from "@reduxjs/toolkit"


const intialState = {
   
}
    
const users = createSlice({
    name: "user",
    initialState,
    
    reducers: {
     addNewProduct: (state) => {
        
     }
    }
})