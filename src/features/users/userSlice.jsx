import { createSlice } from "@reduxjs/toolkit"


const intialState = {
   
}
    
export const users = createSlice({
    name: "user",
    initialState,
    
    reducers: {
     addNewProduct: (state) => {
        
     }
    }
})